import StyleguideLayout from './StyleguideLayout'
import Palette from './tokens/Palette'
import FontType from './tokens/FontType'
import FontSize from './tokens/FontSize'
import LetterSpacing from './tokens/LetterSpacing'
import BoxShadow from './tokens/BoxShadow'
import BorderRadius from './tokens/BorderRadius'
import BorderWidth from './tokens/BorderWidth'
import { ds } from "styles/tokens"

<StyleguideLayout
  elements={[
    {
      label: 'Palette',
      id: 'colors'
    },
    {
      label: 'Box shadows',
      id: 'shadows'
    },
    {
      label: 'Border radius',
      id: 'radius'
    },
    {
      label: 'Border widths',
      id: 'widths'
    },
    {
      label: 'Letter spacings',
      id: 'letterspacings'
    },
    {
      label: 'Font types',
      id: 'fonttypes'
    },
      {
      label: 'Font sizes',
      id: 'fontsizes'
    }
  ]}
>

  # Tokens
  <a id="tokens"></a>

  ## Colors
  <a id="colors"></a>
  Classes: `text-colorName`, `bg-colorName`, `border-colorName`
  <div style={{marginTop: "30px", marginBottom: "60px"}}>
    <Palette colors={Object.keys(ds.get('colors'))} />
  </div>

  ## Shadows
  <a id="shadows"></a>
  Class: `shadow-value`
  <div style={{marginTop: "30px", marginBottom: "60px"}}>
    <BoxShadow shadows={Object.keys(ds.get('shadows'))} />
  </div>

  ## Borders
  <a id="borders"></a>

  ### Radius
  <a id="radius"></a>
  Class: `rounded-value`
  <div style={{marginTop: "30px", marginBottom: "60px"}}>
    <BorderRadius radius={Object.keys(ds.get('radius'))} />
  </div>

  ### Width
  <a id="widths"></a>
  Class: `[border-style] [border-color] border-value`
  <div style={{marginTop: "30px", marginBottom: "60px"}}>
    <BorderWidth widths={Object.keys(ds.get('borderWidths'))} />
  </div>

  ## Typographies
  ### Letter spacing
  Class: `tracking-typeName`
  <a id="letterspacings"></a>
  <div style={{marginTop: "30px", marginBottom: "60px"}}>
    <LetterSpacing spacings={Object.keys(ds.get('type.spacings'))} />
  </div>

  ### Types
   Class: `font-typeName`
  <a id="fonttypes"></a>
  <div style={{marginTop: "30px", marginBottom: "60px"}}>
    <FontType types={Object.keys(ds.get('type.fontFamily'))} />
  </div>

  ### Sizes
   Class: `text-sizeValue`
  <a id="fontsizes"></a>
  <div style={{marginTop: "30px", marginBottom: "60px"}}>
    <FontSize sizes={Object.keys(ds.get('type.sizes'))} />
  </div>

</StyleguideLayout>